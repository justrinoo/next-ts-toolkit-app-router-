import RootLayoutLogin from '../(layout)/index';

export default function Login({ children }: { children: React.ReactNode }) {
    return (
        <RootLayoutLogin>
            <h1>Hello</h1>
        </RootLayoutLogin>
    );
}
